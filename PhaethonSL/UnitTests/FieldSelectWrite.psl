struct foo
{
    word a;
    word b;
};

foo fooVar;

void TestFunction(word v)
{
    fooVar.b = v;
}

void main()
{
    fooVar.a = 1;
    fooVar.b = 2;
    __emit(doutr, fooVar.a);
    __emit(doutr, fooVar.b);
}

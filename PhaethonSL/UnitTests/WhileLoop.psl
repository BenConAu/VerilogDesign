void main()
{
    word i = 10;
    while (i != 0)
    {
        __emit(doutr, i);
        i = i - 1;
    }

    i = 10;
    while (i >. 0)
    {
        __emit(doutr, i);
        i = i - 1;
    }
    
    __emit(doutr, i);
}

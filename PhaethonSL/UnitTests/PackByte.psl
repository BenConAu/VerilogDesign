void main()
{
    word dest = 1234;
    __emit(doutr, dest);
    packbyte(dest, 127, 0);
    __emit(doutr, dest);
    packbyte(dest, 63, 1);
    __emit(doutr, dest);
    packbyte(dest, 31, 2);
    __emit(doutr, dest);
    packbyte(dest, 15, 3);
    __emit(doutr, dest);
}

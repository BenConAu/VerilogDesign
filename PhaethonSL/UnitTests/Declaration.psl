struct foo
{
    word a;
};

foo fooVar;

void main()
{
    float v1;
    v1 = f0.0;
    float v2 = f1.0;
    float v3 = v2 * f4.0;
    __emit(doutr, v1);
    __emit(doutr, v2);
    __emit(doutr, v3);
}
